version: "3"
services:
  nginx:
    image: nginx:alpine
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - gateway
    ulimits:
      nofile:
        soft: 65535
        hard: 65535

  gateway:
    build: ./gateway
    image: gateway:latest
    environment:
      - NATS_URL=nats://nats:4222
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --workers 2 --log-level warning --no-access-log
    volumes:
      - ./common:/app/common:ro
    depends_on:
      - nats

  nats:
    image: nats:2.12-alpine
    command: ["--js"]

  orchestrator-service:
    build: ./orchestrator
    image: orchestrator:latest
    environment:
      - NATS_URL=nats://nats:4222
    command: python app.py
    env_file:
      - env/orchestrator_redis.env
    volumes:
      - ./common:/app/common:ro
    depends_on:
      - orchestrator-db
      - nats

  orchestrator-db:
    image: redis:7.2-bookworm
    command: redis-server --requirepass redis --maxmemory 512mb

  order-service:
    build: ./order
    image: order:latest
    environment:
      - GATEWAY_URL=http://nginx:80
      - NATS_URL=nats://nats:4222
      - REDIS_SHARDS=order-db-0:6379
    command: python app.py
    env_file:
      - env/order_redis.env
    volumes:
      - ./common:/app/common:ro
    depends_on:
      - order-db-0
      - nats

  order-db-0:
    image: redis:7.2-bookworm
    command: redis-server --requirepass redis --maxmemory 512mb

  stock-service:
    build: ./stock
    image: stock:latest
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_SHARDS=stock-db-0:6379
    command: python app.py
    env_file:
      - env/stock_redis.env
    volumes:
      - ./common:/app/common:ro
    depends_on:
      - stock-db-0
      - nats

  stock-db-0:
    image: redis:7.2-bookworm
    command: redis-server --requirepass redis --maxmemory 512mb

  payment-service:
    build: ./payment
    image: payment:latest
    environment:
      - NATS_URL=nats://nats:4222
      - REDIS_SHARDS=payment-db-0:6379
    command: python app.py
    env_file:
      - env/payment_redis.env
    volumes:
      - ./common:/app/common:ro
    depends_on:
      - payment-db-0
      - nats

  payment-db-0:
    image: redis:7.2-bookworm
    command: redis-server --requirepass redis --maxmemory 512mb
